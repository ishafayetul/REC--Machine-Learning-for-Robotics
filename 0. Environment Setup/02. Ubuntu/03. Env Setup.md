## 📝 Dev Environment Note
This project uses a local virtual environment for all Python and ML work.
### Create & Activate

From the project root:

```bash
python3 -m venv .venv
source .venv/bin/activate
```
### Install Core Dependencies
```bash
pip install --upgrade pip
pip install numpy pandas matplotlib scikit-learn jupyter ipykernel
```
### Install PyTorch with GPU Support (Only for Deep Learning)
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```
### Register Jupyter Kernel
```bash
python -m ipykernel install --user --name=ml --display-name="ML (.venv)"
```
### Install Extensions
- Python (MS)
- Jupyter (MS)
### Select the interpreter
```
Ctrl+Shift+P → Python: Select Interpreter → choose the one from .venv
```
### Quick Verification
```bash
import sys, numpy, pandas, sklearn
print(sys.executable)
print(numpy.__version__)
print(pandas.__version__)
print(sklearn.__version__)
```